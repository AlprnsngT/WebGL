 // Create a WebGL examples 
 var canvas = document.getElementById('canvas');
 var gl = canvas.getContext('webgl');

 // Bacground Color
 gl.clearColor(0.178, 229, 255, 1);

 // Clear Buffer
 gl.clear(gl.COLOR_BUFFER_BIT);